import {
  eachIndexed
} from '../../../helpers/each-indexed';
import { module, test } from 'qunit';

module('EachIndexedHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = eachIndexed(42);
  assert.ok(result);
});
